# 2026-09-08 (화) · 2주차

## 한 일

**서울 25개 자치구 수집 완료.** 8개 구만 받아 두었던 실거래 자료를 서울 전역으로
넓혔다. 통제군 후보와 합성대조군 기증자 풀을 만들려면 25개 구가 다 있어야 한다.

| 범위 | 표본 | 단지 | 법정동 | 자치구 |
|---|---|---|---|---|
| analysis | 472,782 | 3,502 | 108 | 8 |
| seoul | 1,407,186 | 9,240 | 343 | 25 |

**`02_build.py` 에 `--scope` 를 붙였다.** 자료를 넓히면서 기존 분석 표본이 조용히
바뀌는 것이 제일 무서웠다. 범위를 이름으로 고정하고 산출물 경로를 갈라
(`TRADES` / `TRADES_SEOUL`) analysis 결과가 한 행도 안 움직이는 것을 확인했다.

**지정 이력을 일 단위 구간으로 바꿨다.** `scripts/build_ltz_spells.py`.
공고 한 건 = 한 행이던 것을 (법정동, 시작일, 종료일, 상태) 의 구간 기록으로
다시 짰다. 자치구 전역 지정은 그 구의 모든 법정동으로 펼친다(44행 → 801행).

- `ltz_spells.csv` 645구간 · `ltz_events.csv` 1,290전이 · `ltz_panel.csv` 월 패널
- 패널 168개 법정동, 지정 5,125개월 / 불명 933개월

**처치군 후보를 자동으로 줄 세웠다.** `scripts/rank_candidates.py`.
(자치구, 시작월, 계열) 로 묶고 사전 36개월 무지정 · 사후 12개월 이상 ·
통제동 2개 이상 · 유효표본 500건 이상으로 거른 뒤, 지정방식(동전체 → 혼합 →
일부) · 사후개월 · 유효표본 순으로 정렬한다. 결과를
`docs/design/처치군-후보표.md` 로 뽑는다.

**결정기록 0011(통제군 선정), 0012(처치군 우선순위)** 작성.

**법정동별 지정 이력 히트맵** (`scripts/plot_ltz_heatmap.py`).

## 발견

**자치구 상태가 공고 사이로 새고 있었다.** 국토부 공고는 경기도 구역을 같이
싣는데, 스캐너가 `cur_gu` 를 계속 들고 가는 바람에 하남시 교산동이 "강동구
교산동"으로 들어왔다. 지형도면 캡션도 앞 표의 자치구를 물려받아, 우리가 직접
읽은 2020-1832호에서 "송파구 대치동"이 나왔다.

시/군 머리글과 '붙임'·'별첨'에서 상태를 비우는 `RESET` 규칙을 넣었다.
법정동 기준표와 대조했을 때 불일치가 **46건 → 3건**으로 줄었다.

**`목동` 이 `면목동` 에 걸렸다.** 계열 분류 정규식에 `(?<![가-힣])` 를 붙였다.
한글은 단어 경계(`\b`)가 듣지 않는다는 것을 이번에 몸으로 배웠다.

**단독 배포본 2건을 지웠다.** 같은 공고가 관보본과 단독 PDF 두 벌 있었다.
결정기록 0010의 인용을 시보본으로 바꾸고 중복을 정리해 출처를 하나로 모았다.

## 다음

6b 사건 시점 정렬. 그다음 6c.
